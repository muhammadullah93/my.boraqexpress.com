{
  "name": "sellflow-commerce-os",
  "version": "0.3.0",
  "private": true,
  "description": "Role-based ecommerce operations platform with fulfilment, returns, wallets, reports and PackProof workflows.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && find src public test -type f -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.14.3"
  }
}
